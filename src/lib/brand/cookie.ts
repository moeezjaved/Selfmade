/** The cookie name that remembers the active project (brand) app-wide. Client-safe (no next/headers),
 *  so both client components (ProjectSwitcher) and server code can import it. */
export const BRAND_COOKIE = 'sf_brand'
